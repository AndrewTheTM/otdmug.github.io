# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Ohio Travel Demand Model User Group
email: andrew@siliconcreek.net
description: >- # this means to ignore newlines until "baseurl:"
  This is the website for the Ohio Travel Demand Model User Group, a group of 
  public agency staff, consultants, and vendors that meets periodically to 
  further the goal of better transportation analysis methods.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://www.otdmug.org" # the base hostname & protocol for your site, e.g. http://example.com
don_scott_map_url: "https://maps.app.goo.gl/EUSoW8HXh7bbxxsQ8"
# twitter_username: hambrew807
# github_username: AndrewTheTM

# Build settings
plugins:
  - jekyll-feed
  - jekyll-remote-theme

remote_theme: AndrewTheTM/otdmug_jekyll_theme

permalink: /content/:title.html

defaults:
  - scope:
      path: "" # "" applies to all files in the project
    values:
      layout: "post" # Use the name of your layout in _layouts/

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
   - .jekyll-cache/
   - gemfiles/
   - Gemfile
   - Gemfile.lock
#   - .sass-cache/
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
